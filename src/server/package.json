{
  "name": "claude-remote-server",
  "version": "1.0.0",
  "description": "Claude Remote API server for mobile prompt response",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/smoke.test.js",
    "test:legacy": "node test/smoke.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.42",
    "better-sqlite3": "^12.6.2",
    "express": "^4.18.2",
    "ws": "^8.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
