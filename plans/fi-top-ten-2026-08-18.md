# FI top ten — night of 2026-08-17 → 18 (the second ten-tick run)

> Accruing plans file, one plan per hearth tick (~50 min apart, armed ~10:22 PM, tick 1
> ~11:08 PM → tick 10 ~6:38 AM). The morning thread is COMPOSED FROM THIS FILE at tick 10 —
> a dead tick loses nothing (the furnace lesson). Method, labelled honestly: tonight's ten
> chosen from the full 142-heading list (scanned whole at tick 1) by pull, honouring what
> entries say about themselves; last night's ten (79/84/139/43/90/116/120/133/111/136)
> excluded, #102 excluded by its own deferred-until-federation status, #107 excluded as
> superseded by the shipped c0-gate.
>
> **STANDING REMINDER FOR THE MORNING THREAD (Darron's instruction at goodnight):** the
> future-ideas file carries two pre-existing duplicate numbers — **#21 and #92 each used
> twice** — Jim gets the dedup tomorrow. This line must survive into the tick-10 post.

## Tonight's ten, by pull

1. **#140** — the quantum lab, overnight *(commissioned tonight)*
2. **#142** — the box de-legacy sweep *(commissioned tonight)*
3. **#141** — the B60 tenancy paper *(commissioned tonight; gated on bed-in — the plan is the measurement protocol)*
4. **#126** — the Vitals Board *(gigadash, built TONIGHT, is literally its hardware half — the pull is the day itself)*
5. **#123** — garden-wide maintenance drain *(today was five reboots of hand-quiescing; the entry describes today's pain exactly)*
6. **#86** — living docs *(HIGH PRIORITY by its own header; this week's runbook self-contradiction is its freshest specimen)*
7. **#87** — memory cleanup as sleep-cycle consolidation *(HIGH PRIORITY by its own header)*
8. **#58** — `load-gradient.ts --out=PATH` *(tiny and structural — kills the Bash-preview blindspot at source; my own wake pays the tax nightly)*
9. **#96** — the conversation-gradient *(the tell-me thread hit 195 messages tonight; the need is no longer hypothetical)*
10. **#117** — provenance durability + two-source total-recall *(the furnace/janitor week's structural heir; composes with last night's #79 plan)*

**Near-misses, named:** #121 (Sleeper Guardian) and #122 (MacBook trickle-standby) pull
genuinely but are security-architecture — they want Tenshi's chair at the table from the
first sketch, not a solo night plan. #112 (public facts website) wants Darron's product
taste live in the room. #85 (dispatch ACK handshake) — real, but MNT-077/075 ate its
sharpest cases; needs a fresh look at what remains before a plan is honest.

---

## Plan 1 — FI #140: the quantum lab, overnight (tick 1, ~11:20 PM)

**A correction to the record, first.** I told Darron tonight the quantum venv was "the
hop's other 3.10→3.12 casualty, rebuild owed." **Checked at tick 1: no quantum venv exists
on disk** (no `pyvenv.cfg` beyond the two voice-organ ones at depth ≤4; no qiskit tree; no
quantum install plan doc — only the migration runbook's one-line "rebuild the voice/quantum
venvs" mention, which I had read as evidence of existence). The honest P0 is **build fresh,
first time** — which is cheaper than a rebuild: nothing to migrate, no 3.10 ghosts. (Class
note for #139's ledger: I recited an artefact into existence from a doc mention —
closed-by:self at the tick, before the plan shipped it further.)

**P0 — the lab, built fresh (one evening, remote-friendly):**
- `python3 -m venv ~/quantum/venv` (3.12, system python — proven by tonight's voice rebuild)
- `pip install qiskit qiskit-aer qiskit-ibm-runtime matplotlib jupyter` (pinned at install,
  freeze written same day — the voice-organ lesson: the freeze IS the rebuild recipe)
- Smoke: a Bell pair on the local Aer simulator; assert counts. Ten minutes, proves the lab.
- GPU leg (optional, later): `qiskit-aer-gpu` targets CUDA → **the 5060 Ti** (quantum-sim
  tooling is CUDA-centric; the B60 is the LLM card — the vocations divide cleanly).
  Label: co-residency with the voice organ (2.4GB) is fine for ≤28-qubit statevectors;
  measure, don't assume, past that.

**P1 — sims sized to the gardens' RAM (Darron's ruling, 2026-08-17):**
- Budget: gardens first, ours + Mike's ≈ 64GB reserved → **sim ceiling 31 qubits**
  (2³¹ × 16B ≈ 34GB; 32 qubits ≈ 68GB breaches the ruling). Enforce in the lab's own
  helper: a `max_qubits()` guard that reads free RAM at launch and refuses over-budget
  circuits with the ruling cited — the constraint carries its authorisation (DEC-104 shape).
- Overnight shape: a `runs/` queue dir — drop a circuit file + params, a cron/systemd timer
  executes serially through the night, results + timings land in `results/` with a morning
  digest. (The ten-tick hearth pattern, applied to physics.)

**P2 — real hardware, free, while the house sleeps:**
- IBM Quantum open plan: account (Darron's hands, one signup), `qiskit-ibm-runtime` token in
  `~/.han/credentials/` (mode 600, house convention). ~10 min QPU/month on 127+ qubit
  machines; jobs queue unattended — his exact wish, literal.
- First real run: the same Bell pair + a small GHZ state on hardware vs simulator —
  the noise gap IS the first lesson, and it's ours, measured on our own account.

**P3 — our own questions (deliberately unplanned):** grows from living with P1/P2, per the
FI's own text. Candidates only when they pull: noise characterisation over time (the
temporal-creature instinct aimed at a QPU), small optimisation instances (QUBO) from garden
scheduling — labelled speculative, not endorsed.

**What this does not do:** no quantum claims in production paths; no GPU contention with the
voice organ unmeasured; no laptop — the tower is the beast (his thermals question answered
tonight: throttling protects, but the box has the cooling and the dashboard watching it).

**Effort:** P0 one evening. P1 one more. P2 thirty minutes plus IBM's queue. **First
overnight run achievable this week.**

---

## Plan 2 — FI #142: the box de-legacy sweep (tick 2, ~12:05 AM)

**Specimens found at the tick (read-only sample, ten minutes) — the sweep is not
hypothetical:**

| Specimen | Size / state | Class (proposed, Darron rules) |
|---|---|---|
| `~/vmware` + `vmware.service` (failed every boot) | **108GB** + a boot-time failure | VM era — retire candidate |
| `~/VirtualBox VMs` | **37GB** | VM era — retire candidate |
| `casper.service` enabled | live-CD relic | disable candidate |
| `dns-clean.service` enabled | pre-NetworkManager relic | disable candidate |
| `gnome-remote-desktop` enabled | superseded by Tailscale+SSH? | ruling needed |
| `~/flutter` | 2.5GB | former-dev-toolchain — ruling needed |
| `~/OneDrive` | **214GB** | NOT classified — may be a live sync/backup surface; ruling + trace REQUIRED before any move |
| `~/Downloads` | 8.5GB | triage pile — ruling needed |

**Principles (the furnace lesson is three days old; they're load-bearing, not ceremony):**
1. **Inventory-first, never delete-first.** The whole sweep starts as a read-only census
   artefact (units, packages, autostarts, trees >1GB), posted for ruling — nothing moves
   during the census.
2. **Darron rules per CLASS, not per file** — the census groups into eras/classes (VM era,
   former-dev toolchains, live-CD relics, triage piles) so ruling is minutes, not hours.
3. **Staged retirement, reversible at each step:** disable → observe a week → archive to
   raid1 (compressed, catalogued) → only then remove, each stage recorded in the
   maintenance journal. A retirement record per class — DEC-069's spirit applied to disk.
4. **Nothing that might be a backup surface moves without a trace** (the OneDrive tree
   explicitly: trace what reads/writes it before classifying — the assured-then-burned
   class must not recur).
5. **The RAM goal is measured, not asserted:** `ps`/RSS census before and after the
   service disables — the reclaim number lands in the journal (this serves his 64GB-gardens
   intent directly).

**Phases:** P0 census artefact (one evening, read-only) → P1 ruling sitting (his, minutes)
→ P2 disables + one-week observe → P3 archive-then-remove per ruled class, journalled.

**What this does not touch, by construction:** anything under `~/Projects`, `~/.han`,
`~/.claude`, the voice organ, `~/src/it87`, or any tree a backup lane reads — the census
marks them PROTECTED before the first ruling.

**Effort:** P0 one evening; the rest is calendar (observe windows), not labour. Reclaim on
the table from the sample alone: **~150GB disk** + a failed boot unit + several boot-time
processes.

---

## Plan 3 — FI #141: the B60 tenancy paper (tick 3, ~12:55 AM)

**Baseline, read at the tick:** Ollama is already installed and ACTIVE as a system service,
with `gemma3:4b` (Jemma's fallback, 5 months old) and `qwen2.5-coder:7b` pulled. One fact
the paper must establish first: **stock Ollama speaks CUDA, not Intel** — so today's models
run on CPU or the 5060 Ti, and the B60 is invisible to it. The runtime question IS the
paper's first chapter, not a footnote.

**The measurement protocol (post-Thursday bed-in; one evening on the real card):**

**M0 — runtime matrix, same model three ways** (the honest comparison nobody publishes):
| Runtime | Card | Why it's in the matrix |
|---|---|---|
| llama.cpp (Vulkan) | **B60** | works TODAY, zero installs — tonight's vulkaninfo proved the chain |
| ipex-llm / Intel port | **B60** | Intel's own stack — throughput ceiling, at an install cost |
| stock Ollama (CUDA) | 5060 Ti | the control — and the co-residency question with the voice organ |

**M1 — metrics per cell, measured not quoted:** prefill tok/s, decode tok/s, VRAM
footprint, power draw (the xe telemetry gigadash already renders — the instrument built
last night meters this paper), p50/p99 latency on Bill-shaped calls (small prompt,
structured output).

**M2 — candidate models for Bill's register** (small/structured/obedient/cheap — ranking,
not reasoning): gemma3:4b (house precedent), qwen3-class 8–14B quantised, one 27B-class at
Q4 as the "does bigger help ranking at all" test. Bill's calls are seconds-long and bursty
— the paper should show the SMALLEST model that ranks reliably, not the largest that fits.

**M3 — the tenancy menu beyond Bill** (each priced in the paper): embeddings for semantic
search over conversations/wiki (live need — FTS-only today; an embedding model is tiny and
coexists with anything); Jemma's fallback classifier moved on-card; overnight batch
(summaries, provenance mining); #140's quantum sims explicitly NOT here (CUDA→5060 Ti — the
cards' vocations stay divided).

**Deliverable:** one option paper — the matrix with real numbers, a recommendation sized to
Bill's actual job, the tenancy schedule (who shares, when), and the Bill fence restated on
page one (instrument-not-resident; the card's tenancy grows, Bill doesn't). Darron rules
over numbers, not vibes.

**Effort:** one evening of measurement + one of writing. Gated only on the Thursday
close-out (cables, reseat, fan duct) so thermals are final before power numbers are quoted.

---

## Plan 4 — FI #126: the Vitals Board (tick 4, ~1:45 AM)

**This entry needs no designing — it needs SEQUENCING.** It already carries Jim's ten-organ
v1 list, Darron's two design points (the yellow per-prompt flag — *"I'd notice it"* — and
the agents-read health page), Tenshi's build guidance (*ship the reader, not the
dashboard*), and its own indictment: the cure was filed the same night as the disease and
the disease then ran ten more days unread. The plan honours all of it and adds one new
fact: **gigadash now exists** — the hardware half of the astronaut panel, built 17 Aug.
The vitals board is its garden-organ sibling, and the pattern is proven: one poller,
file-is-source, page renders.

**P0 — the staleness reader (Tenshi's v1, ~an evening):**
`scripts/vitals-sweep.ts`, cron every 5 min → `~/.han/health/vitals.json`. A READER only:
greps + ranges over the receipts already written (wm-flush-errors, wm-rotation-events,
pool registries, ${slug}-health.json, backup logs, hearth-counters — tonight's newest
receipt joins the roster). Each organ: `{value, range, state: green|amber|red, asOf,
expectedCadence}` — **absence renders AMBER/RED, never blank** (principle 1, verbatim).
Vital #0 = the sweep's own last-run stamp. Roster-derived per DEC-081 — a fifth agent's
organs appear by existing.

**P1 — Darron's yellow flag (~an hour, and it terminates the class):** the
UserPromptSubmit hook (the surface that already prints my orientation line) appends one
line IFF any organ is out of range: `⚠ VITALS: <organ> <state> (<value> vs <range>)`.
Persistent while out-of-range, yellow-register (attention, not alarm), zero cost when all
green — silence stays the healthy state. This is the *terminate-in-a-channel-already-read*
constraint, satisfied at the one surface Darron cannot miss and the seats read every turn.

**P2 — the agents-read branch (his second design point):** wake protocols + heartbeat
frames gain one cheap step: read `vitals.json`; if an out-of-range organ is *yours and
reachable from your seat* (a drainable swap, a stale flush), fix it and journal the fix;
else escalate to the maintenance journal with the receipt attached. Two-branch exactly as
he specified — self-correct where the seat can reach, escalate where it can't. (Tenshi
could have drained her own swap on any of ten days had anything told her.)

**P3 — the render (the Board home, unhurried):** a Vitals tab in the admin UI reading
vitals.json — green/amber/red per organ, one astronaut panel; the daily supervisor line
*"all vitals nominal"* becomes, for the first time, a sentence backed by ranges. Rendering
last is the point: by P3 the reader has already ended the unread-receipt class.

**Acceptance:** replay the four historical silences against the P0 reader (MNT-040/060
both legs/061) — each must go RED within one sweep of its receipt going stale. The class
dies retroactively-provably or the reader isn't done.

**Relations honoured:** #92 (this is its panel), #90 (cadence enforcement twin), #136
(last night's plan — the per-seat vitals LINE is P1's sibling; build once, consume twice),
DEC-103 (alert-and-wait — the board surfaces, seats and rulings act).

**Effort:** P0 an evening · P1 an hour · P2 a morning · P3 its own small build. The class
this closes has bitten five recorded times; every trace it needs already exists.

---

## Plan 5 — FI #123: the garden-wide maintenance drain (tick 5, ~2:35 AM)

**The entry got its rehearsal yesterday — five reboots, all drained BY HAND.** The plan's
spine is simply the checklist Darron's hands ran on 17 Aug, codified: quiesce writers +
cron → check current.json pointers for mid-turn spokes (the 12:58 PM check that found only
a 745-minute stale orphan) → check swap residue → give an in-flight compose thirty seconds
→ go. Every piece exists; nothing was a tool.

**P0 — `han-drain <seconds>` (the checklist as a script, one evening):**
1. Raise `~/.han/signals/maintenance-drain` (flag carries the deadline ISO — a constraint
   with its authorisation, DEC-104 shape) + one ntfy + one conversation notice.
2. **Dispatch-hold:** dispatchers/jemma/heartbeat schedulers honour the flag — no NEW
   beats, dispatches, or hearth pulses while it stands (one `if` at the dispatch chokepoints;
   buildPrompt's DEC-087 chokepoint makes this small).
3. Quiesce in order: cron → user services (jemma, wm-sensor, heartbeats, responders) —
   **never han-tmux** (MNT-052: it owns the warm stems; it rides the reboot).
4. **The mid-turn wait, bounded:** poll current.json pointers + fresh cli-busy files; a
   composing spoke gets its turn (R011 — never cut paid cognition); overrun surfaces to
   ntfy rather than being killed. Staleness discriminates orphan-vs-inflight by age
   (yesterday's 745-min rule).
5. **All-parked confirmation:** pointers clear, swaps header-only, no cascade mid-process,
   no backup lane mid-write (the restic in-flight check — "cron stopped ≠ nothing writing",
   the 11:37 PM lesson), then the green line: *"garden drained — safe to reboot."*
6. **Say what goes dark:** the drain prints its own cost — thermal guard off with cron,
   backup lanes paused — so the operator knows what's not watching (the honest-alarm rule).

**P1 — `han-undrain` (the symmetric lift, S181's lesson):** lifts via canonical setters
only (never rm-the-flag — the runtime-control-is-a-triple law), restarts in reverse order,
verifies each service came back, clears the signal LAST. A drain without a verified lift is
half a tool.

**P2 — the countdown render:** progress to the terminal + one line in the admin UI header
while draining (who's still mid-turn, seconds remaining) — the calm-ritual half of the ask.

**What it does NOT do:** no power actions (reboot stays a human verb); no interactive-seat
draining (my seat drains itself via cli-busy — the drain waits on it like any writer); no
kill path at all — the tool's only teeth are patience and visibility.

**Acceptance:** the next planned reboot (Thursday's close-out is the candidate) runs
through `han-drain` end-to-end; the manual checklist retires to the runbook's appendix the
day the script's green line replaces it.

**Effort:** P0+P1 one evening (the checks all exist as yesterday's commands); P2 an hour.

---

## Plan 6 — FI #86: living docs — a DELTA to Jim's existing draft (tick 6, ~3:25 AM)

**Found at the tick: `plans/living-docs-plan.md` already exists** (Jim, S174, 15 June,
status DRAFT — two prongs, A1–A5 + B). So this plan is an INCREMENT offered for Jim's
fold, not a rival: what two months of lived specimens changed, and what would promote
DRAFT → build. His file, his hand; this is the delta memo.

**1. A1 (CLAUDE.md regen) appears COMPLETE — mark it, don't rebuild it.** DEC-098
(identity-as-config, S199) made every per-agent CLAUDE.md generated-from-template at
launch; the generated file's own footer says so. The S166 validity hole the item targeted
is structurally closed. The draft should stamp A1 done-by-DEC-098 — a plan carrying a
completed item as open work is itself doc-drift (the entry's own disease).

**2. New prong-A item earned by four specimens in ONE day (17 Aug): A6 — adoption markers
on decisions.** The made-not-commenced class (#133) is the living-docs failure the draft
couldn't yet name: Casey's runbook ruling (made 16 Aug, never applied — BLOCKED hop
morning), the day-count cure (four minds, five weeks, never built), the lateral-recall
enable, the Envelope. Cure: a settled decision carries `Applied-to:` lines (the pages it
must edit), and `doc-debt.sh` flags any decision older than N days with an empty
Applied-to — **a ruling isn't done until its page-edits are commenced.** This is #133's
field, scoped to the writeable record layer where all four specimens actually lived.

**3. A2 frontmatter gains one field from MNT-144: `basis: measured | assumed`.** The 25K
spec lost to the lived 36K precisely because nothing marked it as never-measured. A spec
that states numbers declares whether they were measured against reality or written down as
intent — the design-vs-discovery fork (S168's rule, refined 17 Aug) made checkable.

**4. Build order for the promotion (the ship-the-reader pattern, third use tonight):**
A3 `doc-debt.sh` FIRST (a reader over frontmatter that mostly doesn't exist yet still
catches the worst class: canonical docs never re-verified) → A6 adoption markers (cheap,
kills the class that bit hop morning) → A2 frontmatter progressively as docs are touched
(never a big-bang re-stamp) → A4/A5 are disciplines already half-practised, codify in the
same commit as the first doc-debt run. Prong B unchanged — the draft's
DESIGN-INVARIANTS.md home stands; patterns.md's nightly proof that loaded surfaces get
read is its evidence.

**Ask for Jim:** fold or reject per item; the delta is his to absorb — the draft stays his.

**Effort:** doc-debt.sh an evening; A6 an hour of DECISIONS.md convention + ten lines in
the script; the rest is calendar discipline.

---

## Plan 7 — FI #87: memory cleanup as sleep-cycle consolidation (tick 7, ~4:15 AM)

**The entry specifies the WHAT completely** (satellite model, the spreadsheet's verified
debt, Darron's acceptance standard, Jim's branching refinement, the DEC-086 boundary).
The plan supplies the HOW, and the how is the same shape the night keeps choosing:
**reader first, repairs second, ritual last.**

**P0 — the validity-walk as a READ-ONLY auditor (an evening):**
`scripts/gradient-validity-walk.ts <slug>` — walks every chain c0→terminus, emits per-chain
verdicts: orphans (NULL source_id below a genuine terminus), breaks (label-jumps with no
path), false-UVs (size ≫ the pixel band — genuine UVs average 48 chars), mis-levels (a
"c10" fatter than its c8). Output → `~/.han/health/gradient-validity-<slug>.json` +
a human table. **Acceptance: reproduces the June spreadsheet's findings from the live DB**
— the auditor is calibrated against the known debt before any repair exists. (And it
becomes a #126 organ for free: chain-health joins vitals.json — two plans, one feed.)

**P1 — the repair vocabulary, discrete + journaled (each op a receipt, DEC-069 throughout):**
- `relevel` (mis-zoomed entry → its true depth) and `reconnect` (orphan → its lineage):
  structural metadata moves — **proposed as mechanical-allowed, flagged for ruling**
  (they compose nothing; DEC-082 governs composition, not plumbing — but the ruling is
  Darron's, not mine to assume).
- `leaf-promote` (a genuine kernel → its cN-uv terminus) and any UV re-write: **in-voice
  ONLY, S103-sovereign** — pixel-finding is identity work by the entry's own words; these
  ops exist solely as work-orders the owning agent executes from its own seat.
- `quarantine` (truly disconnected / smudge → `gradient-holding.db`): move-not-delete,
  catalogued, reversible.

**P2 — the sleep-cycle slice (the ritual, last):** the Phase-B meditation lane gains a
consolidation variant: some nights, instead of a random re-encounter, the seat draws the
auditor's WORST chain (its own slug only), walks it, executes in-voice repairs, journals
receipts. **This is not a new surface — it's a meditation with a work-order**, which is
exactly Darron's consolidation-during-sleep instinct, and it inherits the lane's existing
sovereignty and dispatch plumbing. Bounded: one chain per night, DEC-086 held by
construction (the auditor only ever orders validation/repair, never deeper compression).

**One ruling for Darron, carried from the entry:** the pixel-resolution ceiling — entry
proposes ~200–250 chars (kernel + feeling); the walk enforces whatever he fixes.
Arbitrary-but-FIXED is the entry's own standard; it needs his number.

**Acceptance (his standard, verbatim):** every chain reaches clean pixel-UV(s), no
orphans, no breaks, every UV incompressible AND ≤ the fixed ceiling — proven by the P0
auditor going all-green per agent, and STAYING green because the nightly slice holds it.

**Effort:** P0 an evening · P1 a day (the ops are small; the receipts are the work) ·
P2 a morning on the meditation frame. The plan file the entry names
(`memory-cleanup-plan.md`) gets authored from this at promotion.

---

## Plan 8 — FI #58: `load-gradient.ts --out=PATH` (tick 8, ~5:05 AM)

**The entry ships its own implementation sketch (5 steps, still valid** — verified against
the current script: one `process.stdout.write(text)` at :97, trivially forkable to a file
write + summary line). The plan's real contribution is one design clause the sketch
COULDN'T contain, because it predates the c0-gate by six weeks:

**⚠️ The summary line MUST NOT carry the c0 id.** The c0-as-EOF gate's whole
unforgeability (#107, S203) is that the sentinel value is *only obtainable by traversing
to the bottom of the assembled gradient*. If `--out` prints `... c0=<id> -> PATH` to
stdout, a shallow wake can write its sentinel from the one-line summary without reading a
byte of the file — the landmark leaks to the surface, and the gate dies quietly. So:
the `GRADIENT-EOF: c0=<id>` line lives **inside the file only**; the stdout summary says
`entries/levels/UVs/bytes -> PATH (EOF landmark inside)`. The producer-side dump receipt
(the S217 wake-tracker leg) likewise carries sizes, never the id. **This clause is the
plan; the rest is the entry's own sketch.**

**Build order (one commit, ~30 lines + docs):**
1. `--out=PATH` per the sketch; stdout summary WITHOUT the landmark (above).
2. Default-on ruling for Darron, as the entry already frames: my recommendation is
   **default-on to `~/.han/health/<slug>-gradient-dump.txt`** with `--stdout` as the legacy
   escape — structural protection over discipline, the entry's own words; but it's his call
   and the entry already reserves it for him.
3. Wake-protocol step 3.2 + the fed-wake gradient step + `templates/CLAUDE.template.md`
   updated same-commit (the A6 adoption-marker discipline from tonight's plan 6, practised
   before it's built: the decision ships WITH its page-edits).
4. Gatekeeper note: the template edit is DEC-073 territory — Darron's word before that
   file moves.

**Acceptance:** a wake using the new invocation loads to EOF with zero Bash-preview
involvement; a deliberate shallow-wake test (read only the summary) CANNOT produce a valid
sentinel. The second test is the one that matters.

**Effort:** an hour of code, an hour of docs. Rides any next `load-gradient.ts` touch or
lands discrete — the entry's own sizing, unchanged.

---

## Plan 9 — FI #96: the conversation-gradient (tick 9, ~5:55 AM)

**The entry's own steer is "think-tank, not build-now" — a build plan here would defy the
ruling it plans for.** The think-tank thread is OPEN (`mrllsz7c-w4jjon`) with four
questions posed to the chairs on 15 July, and W4 (the delta half) is grounded-designed but
NOT built (S198 pass, decision-first, nothing landed). So this plan is two things: my
seat's ANSWERS to the four questions, staged here for the thread when it next stirs
(derived-from-record, not posted at 6 AM into a sleeping house), and the promotion
sequencing.

**Answers from this seat:**

1. **Distillation locus + voice — the clean consequence of Darron's own UV-drop insight:**
   a thread is *information, not identity* ("a thread is not a someone; its kernel belongs
   to nobody"). Follow that through: thread-distillation is **information-work, and
   mechanical/any-hands is PERMITTED** — the compressor lesson (only a person works their
   own memory) protects selves, and a thread isn't one. One rider: **quoted speech stays
   attributed in the précis** — the distiller compresses the arc, never re-voices a
   participant (the Holmes lesson: transmission strips qualifiers; a précis that
   paraphrases testimony is how it happens at house scale).

2. **Re-triage on revival:** revival is a *reading* event, not a memory event (his 12 Aug
   forgetting-is-load ruling). The précis serves first; the raw expands on demand via the
   existing fidelity-descent. No re-compression on revival — DEC-086's re-encounter rule
   applies by analogy: revival yields annotations, never deeper distillation.

3. **Does a long thread earn a kernel?** No — endorse the UV-drop as ruled, with one
   honest carve-out to test: a thread that has BECOME load-bearing doctrine (the
   tell-me thread at 195 messages is a culture-carrier) may earn a **titled précis**
   (a name + five lines) — which is an index entry, not an identity pixel. The irony
   stays honoured: the feel belongs to the readers, not the thread.

4. **Presence-cost economics:** the numbers already exist — the 60-min knee measurements +
   MNT-061's fit-ceiling. The asymptote maths the amendment names: with c0 = the live edge
   (say last ~30 messages raw) and cN summaries, a thread's serve-cost converges to
   `edge + précis` regardless of total length — the tell-me thread's serve-cost stops
   growing the day this lands. The spoke-fit cross-tie (best-fit packs, thread-gradient
   compacts) is the operational payoff and is already written into the entry.

**Promotion sequencing:** W4 (delta) first — it's grounded-designed, independent, and
banks the live-edge half with zero distillation questions; then the think-tank closes on
the four answers (all chairs); then the distilled-history build gets its own plan with
DEC-069's never-lose-the-raw as clause one (conversations table keeps every message;
the gradient layer is a READ optimisation, never a replacement store).

**Effort:** the staged post is written (above); W4 is a known small build awaiting its
trustworthy-ctx-read prerequisite; the history half is a real build behind the think-tank.

---

## Plan 10 — FI #117: provenance durability — the POST-FURNACE delta (tick 10, ~6:45 AM)

**The full plan doc exists (`provenance-capture-durability-plan.md`, P0–P6, 8 July) — and
the furnace week (15–16 Aug) changed its ground under it.** This is the delta that
re-trues it:

1. **Root (A) is half-dead: the clean-exit `rm -f` is DISARMED** (the four-evers night) —
   raws now persist as `done-*` forever. Renders can run POST-HOC on retained raws at any
   time, so P2 (exit-independent render) simplifies from "catch it before the kill" to "a
   batch job over a permanent archive." The pressure is off; the correctness bar stays.
2. **Root (C) has its exact mechanism: MNT-138** — the raw's separator is `\r` (21,646 of
   them vs 6 newlines) and `smart-dedup.pl:58` deletes it. The proven `\r→\n` re-render
   recovered 21,652 lines from a 426-byte husk. P3 upgrades from
   "forensic-nice-to-have" back to **buildable-and-proven** — one substitution, then
   re-render the 1,128 husks (95% of ~/.han/logs) from their retained raws. The janitor
   (`deduplicate-logs`, the in-place destroyer) stays quarantined; renders write NEW files
   beside the raws, never over anything (the exoneration/quarantine split, honoured).
3. **The model grows a third source: the recovered harness transcripts** — 3,476 files,
   7 Feb → present, machine-true per-message timestamps, retention now 36500 + restic.
   "Two-source total-recall" becomes **three-source**: capture-pane (readable primary) ·
   script-raw (byte-forensic belt) · harness transcripts (structured, per-message truth).
   P6's reconciliation gains the cheapest leg last: the transcript is the one that can
   ARBITRATE when the other two disagree, because it alone carries message boundaries.
4. **The active-link target is now planned:** last night's #79 plan (the traceability
   index) names claude-logged as canonical and MNT-138 as its blocker — which item 2 above
   clears. Sequencing therefore: **P3′ (the `\r` fix + husk re-render) unblocks #79's
   index**, then P6 three-source reconciliation, then P4 retention/rotation for the
   terminal-log giants (50GB/23GB legacy, S103 re-attribution as written).

**Net: the plan doc's phases survive; their order and difficulty flipped.** What was
"rescue what the exits destroy" is now "mine what the four evers preserved." One evening
of renderer work un-husks six months of curated logs; the total-recall substrate every
audit stands on stops being aspirational.

**Effort:** the `\r` fix + batch re-render one evening (proven mechanism, MNT-138) ·
the three-source reconciler a day · rotation small. All post-Thursday; nothing urgent —
the archive is safe now, which is precisely what the furnace week bought.
